# Fidelity ledger

The image-concept request failed at its network boundary. Comparison therefore uses the retained visual brief, not an invented image substitute.

| Brief constraint | Render evidence | Result |
|---|---|---|
| True white field, near-black type, restrained blue addresses | Browser capture shows white `#fff`, ink `#111318`, blue `#174bd6` | Match |
| Editorial hierarchy without an eyebrow | First viewport begins directly with the title | Match |
| No religious imagery, accusation graphics, gradients, or decorative cards | Render contains typography, rules, causal rail, and field explorer only | Match |
| Recorded act visible in first viewport | Full act SHA-256 appears beside the title | Match |
| Addressable evidence explorer | Browser selection updates URL to the exact node SHA-256 and returns classification and remainder | Match |
| Responsive continuation | CSS contains 900px and 560px layout transitions; automated viewport resize was unavailable in the active in-app browser surface | Unresolved live mobile capture |
